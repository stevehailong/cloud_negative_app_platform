server:
  port: 8080
  mode: debug

database:
  dsn: root:root123456@tcp(mysql:3306)/iam_db?charset=utf8mb4&parseTime=True&loc=Local&collation=utf8mb4_unicode_ci
  maxIdleConn: 10
  maxOpenConn: 100

redis:
  host: redis
  port: 6379
  password: ""
  db: 0

jwt:
  secret: my-cloud-secret-key-change-this-in-production
  expireTime: 86400

log:
  level: info
  path: ./logs
