{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mycloud-app.serviceAccountName" . }}
  labels:
    {{- include "mycloud-app.labels" . | nindent 4 }}
{{- end }}
